<section>
  <title><xref ref="S_inversefunctions"></xref></title>
  <subsection>
    <title><xref ref="prog_ordered"></xref></title>
    <p>
      Neither set can be used to define a function.
      <ol>
        <li>
          <p>
            The set <m>F</m> does not satisfy the first condition of <xref ref="T_functionasordered"></xref>.
          </p>
        </li>
        <li>
          <p>
            The set <m>G</m> does not satisfy the second condition of <xref ref="T_functionasordered"></xref>.
          </p>
        </li>
      </ol>
    </p>
  </subsection>
  <subsection>
    <title><xref ref="prog_exploringinverse"></xref></title>
    <ol>
      <li>
        <p>
          <m>f^{ - 1} = \left\{ {( {r, a} ), ( {p, b} ), ( {q, c} )} \right\}</m>
          <m>g^{ - 1} = \left\{ {( {p, a} ), ( {q, b} ), ( {p, c} )} \right\}</m> <m>h^{ - 1} = \left\{ {( {p, a} ), ( {q, b} ), ( {r, c} ), ( {q, d} )} \right\}</m>
        </p>
      </li>
      <li>
        <ol>
          <li>
            <p>
              <m>f^{ - 1}</m> is a function from <m>C</m> to <m>A</m>.
            </p>
          </li>
          <li>
            <p>
              <m>g^{ - 1}</m> is not a function from <m>C</m> to <m>A</m> since
              <m>( {p, a} ) \in g^{ - 1}</m>and <m>( {p, c} ) \in g^{ - 1}</m>.
            </p>
          </li>
          <li>
            <p>
              <m>h^{ - 1}</m> is not a function from <m>C</m> to <m>B</m> since
              <m>( {q, b} ) \in h^{ - 1}</m> and <m>( {q, d} ) \in h^{ - 1}</m>.
            </p>
          </li>
        </ol>
      </li>
      <li>
        <p>
          In order for the inverse of a function
          <m>F:S \to T</m> to be a function from <m>T</m> to <m>S</m>,
          the function <m>F</m> must be a bijection.
        </p>
      </li>
    </ol>
  </subsection>
</section>